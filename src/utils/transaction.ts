import { useWallet } from "@solana/wallet-adapter-react";
import { sendAndConfirmRawTransaction } from "@solana/web3.js";

export default async function sendPaymentTxn(
  fromAddress: string,
  toAddress: string,
  amount: number
) {}
